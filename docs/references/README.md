# Reference PDFs (local only)

Place **Hootsuite** `HootsuiteSocialTrends2026_Report_en.pdf` in this folder for human + LLM prompt guidance.  
**Do not commit** large vendor PDFs — the path is listed in `requirements.md` and ignored by `.gitignore`.
